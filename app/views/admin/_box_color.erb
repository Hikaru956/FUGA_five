<div class="col">
    <div class="card h-100">
        <div class="card-body text-center">
            <small>
                <%= (color.first?)?	move_to_higher_icon(false):	link_to(move_to_higher_icon, 	:action=>"color_higher", :id=>color) %>
                <%= color.position %>
                <%= (color.last?)? 	move_to_lower_icon(false): 	link_to(move_to_lower_icon, 	:action=>"color_lower",	:id=>color) %>
            </small>
        </div>

        <% key_photo = color.photo %>
        <% unless key_photo.blank? %>
            <%= image_tag key_photo.clip.url(:thumb) %>
        <% else %>
            <img src="https://placehold.jp/22/cfcfcf/ffffff/320x180.png?text=No%20Photo%0A">
        <% end %>

        <div class="card-body">
            <h5 class="card-title">
                <%= truncate(model_name(color), :length=>50) %>
            </h5>
            <p class="card-text">
                <small>
                    <%= icon_scheme_state(color) %>
                    <%= h truncate(color.description, :length=>20) %>
                </small>
            </p>
        </div>

        <div class="card-footer">
            <span class='float-right'>
                <% if color.position==1 %>
                    <i class='fa-solid fa-spinner fa-spin'></i>
                    &nbsp;
                <% end %>
                <a href="<%= url_for(:action=>'color_show', :id=>color) %>" class="btn btn-sm  btn-outline-primary"><%= link_to_6_icon %></a>
            </span>
        </div>
    </div>
</div>

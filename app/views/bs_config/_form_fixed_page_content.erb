<%= render :partial=>"layouts/tiny_mce", :locals=>{:target=>@item} %>

<div class="control-group">
	<label class="control-label" for="item_title">タイトル</label>
	<div class="controls">
		<%= text_field 'item', 'title', :autocomplete => "off" %>
	</div>
</div>

<div class="control-group">
	<label class="control-label" for="item_description_2">説明文</label>
	<div class="controls">
		<%= text_field 'item', 'description_2', :class=>'span8', :autocomplete => "off" %>
	</div>
</div>

<legend>ページコンテンツ</legend>
<div class="control-group">
	<%= text_area  'item', 'description', :class => "tinymce input-block-level", rows: 40, :placeholder=>"本文を入力します。" %>
	<script type="text/javascript">
		tinyMCE.init({
			selector: 'textarea.tinymce',
			cache_suffix: "?v=5.10.2",
			theme_advanced_toolbar_location: "top",
			theme_advanced_statusbar_location: "bottom",
			theme_advanced_buttons3_add: ["tablecontrols"],
			plugins: "table,image,code",
			<% unless @item.blank? %>
				<% unless @item.photos.blank? %>
				image_list: [ 
					<%= photo_array(@item.photos).html_safe %>
					],
				<% end %>
    		<% end %>
			relative_urls : false,
    		convert_urls: false,
			toolbar: ["undo redo | blocks | formatselect | fontselect | fontsizeselect | bold | italic | underline| image | table | code | alignleft | aligncenter | alignjustify | alignright"],
			language: 'ja'})
	</script>
</div>

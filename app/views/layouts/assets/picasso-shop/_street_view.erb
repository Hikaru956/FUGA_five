<% streetview = VisualWidget.search_widget_bag_for(@website, "e4bae25bad95b365573ffbac56abd4a2") %>
<% if !streetview.blank? && streetview.is_valid? %>
        <aside>
            <div class="container">
                <div class="content_box">
                    <%= raw streetview.data_string %>
                </div>
            </div>
        </aside>
<% end %>

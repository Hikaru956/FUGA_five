<% widget_bag = VisualWidget.search_widget_bag_for(@website, "6b9a956070f12749714a7f9290a46947") %>
<% if !widget_bag.photos.blank? %>
        <aside>
            <div class="container">
                <div class="content_box">
                    <div id="carousel">
                        <ul id="carousel_ul">
    <% widget_bag.photos.each do | photo | %>
                            <li><img src="<%= photo.clip.url(:image) %>"></li>
    <% end %>
                        </ul>
                        <div id="carousel_prev"><i class="fa fa-chevron-circle-left"></i></div>
                        <div id="carousel_next"><i class="fa fa-chevron-circle-right"></i></div>
                    </div>
                </div>
            </div>
        </aside>
<% end %>

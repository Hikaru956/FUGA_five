<%
    glat2 = VisualWidget.search_widget_bag_for(@website, "df78da07b7d00ee25bdc6260e05b823b")
    glng2 = VisualWidget.search_widget_bag_for(@website, "b044b72a7cb3cbe748e3e33cf01e5d93")
    glat3 = VisualWidget.search_widget_bag_for(@website, "13379235ed160e875131a47f3de09423")
    glng3 = VisualWidget.search_widget_bag_for(@website, "097d3b16e6dca6db133a51df0fefae9c")
%>
<% gmkey = VisualWidget.search_widget_bag_for(@website, "65699edf9f3ce2bd0636af0a8344e28e") %>
<% if gmkey.blank? || !gmkey.is_valid? %>
<script type="text/javascript" src="https://maps.google.com/maps/api/js?sensor=false"></script>
<% else %>
<script type="text/javascript" src="https://maps.google.com/maps/api/js?sensor=false&key=<%= gmkey.data_string %>"></script>
<% end %>
<% if !@website.lat.blank? && !@website.lng.blank? %>
<script>
google.maps.event.addDomListener(window, 'load', function() {
    var latlng1 = new google.maps.LatLng(<%= @website.lat %>, <%= @website.lng %>);
    var op1 = {
        zoom: 17,
        center: latlng1,
        mapTypeId: google.maps.MapTypeId.ROADMAP
    };
    var gmap1 = new google.maps.Map(document.getElementById('gmap1'), op1);
    var mark1 = new google.maps.Marker({
        position: latlng1,
        title: 'Salon',
        map: gmap1
    });
});
</script>
<% end %>
<% if !glat2.data_string.blank? && !glng2.data_string.blank? %>
<script>
google.maps.event.addDomListener(window, 'load', function() {
    var latlng2 = new google.maps.LatLng(<%= glat2.data_string %>, <%= glng2.data_string %>);
    var op2 = {
        zoom: 17,
        center: latlng2,
        mapTypeId: google.maps.MapTypeId.ROADMAP
    };
    var gmap2 = new google.maps.Map(document.getElementById('gmap2'), op2);
    var mark2 = new google.maps.Marker({
        position: latlng2,
        title: 'Salon',
        map: gmap2
    });
});
</script>
<% end %>
<% if !glat3.data_string.blank? && !glng3.data_string.blank? %>
<script>
google.maps.event.addDomListener(window, 'load', function() {
    var latlng3 = new google.maps.LatLng(<%= glat3.data_string %>, <%= glng3.data_string %>);
    var op3 = {
        zoom: 17,
        center: latlng3,
        mapTypeId: google.maps.MapTypeId.ROADMAP
    };
    var gmap3 = new google.maps.Map(document.getElementById('gmap3'), op3);
    var mark3 = new google.maps.Marker({
        position: latlng3,
        title: 'Salon',
        map: gmap3
    });
});
</script>
<% end %>
